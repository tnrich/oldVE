if(typeof CONSTANTS === 'undefined') {
	var CONSTANTS = {};
}

if(typeof module === 'object') {
	module.exports = CONSTANTS;
}

CONSTANTS.EVENTS = {
	GET_FULL_SEQUENCE: '__GetFullSequence',
	GET_FULL_DEVICE: '__GetFullDevice',
	POST_CLIENT_OPERATIONS: '__PostClientOperations',
	SERVER_OPERATIONS: '__ServerOperations'
};




















































